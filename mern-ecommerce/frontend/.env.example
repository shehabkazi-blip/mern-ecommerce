# Base URL for the backend API.
# In local dev, Vite's proxy forwards /api to the backend, so this can stay as /api.
# In production build, point this at your deployed API, e.g. https://api.yourdomain.com/api
VITE_API_URL=/api
